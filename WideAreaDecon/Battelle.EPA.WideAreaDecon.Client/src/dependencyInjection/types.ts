import PROVIDER_TYPES from './providers/providers.types';
import CONVERTER_TYPES from './converters/converters.types';

const TYPES = {
  ...PROVIDER_TYPES,
  ...CONVERTER_TYPES,
};

export default TYPES;
